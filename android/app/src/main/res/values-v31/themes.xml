<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31) platform SplashScreen for Piano Teacher.
         No androidx core-splashscreen compat library is used (it is not a
         project dependency); on API < 31 the windowBackground in
         values/themes.xml provides the fallback. -->
    <style name="Theme.PianoTeacher" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/brand_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:windowSplashScreenBackground">@color/brand_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
    </style>
</resources>
